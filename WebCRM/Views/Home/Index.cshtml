@{
    ViewBag.Title = "Home Page";
}

    <div class="row">
        <div class="col-md-12">
            <div class="text-danger">
                @ViewBag.Message
            </div>
        </div>
    </div>

<div class="jumbotron">
        <h1 style="text-align:center">ABC co. Demo App</h1>
        <h1 style="text-align:center">for presentation</h1>
        <p class="lead">
        <p>
            A basic HTML-only CRUD (Create, Read, Update, Delete) example using ASP.Net, MVC 5, Entity Framework 6 and contains the
            following modules:
            <ul>

                <li>@Html.ActionLink("Customers", "Index", "Companies", new { }, new { })</li>
                <li>@Html.ActionLink("Contacts", "Index", "Contacts", new { }, new { })</li>
                <li>@Html.ActionLink("Activities", "Index", "ActivityHistories", new { }, new { })</li>
            </ul>
            The @Html.ActionLink("About", "About") page describes some of the MVC / EntityFramework / Javascript functionality
            used to build this small web app.
        </p>
    </div>


